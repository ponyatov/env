%{
#include "hpp.hpp"
%}
%defines %union { Sym*o; }
%token <o> SYM CMD
%token SAVE
%type <o> ex
%%
REPL : | REPL ex		{ cout << $2->dump(); }
ex : SYM | CMD
ex : SAVE SYM			{ $$=new Saver($2->val); }

